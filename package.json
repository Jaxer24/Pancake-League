{
  "name": "javaproject",
  "version": "1.0.0",
  "description": "Simple Spring Boot application exposing a WebSocket at `/ws` and persisting messages to an in-memory H2 database.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "ws": "^8.19.0"
  }
}
